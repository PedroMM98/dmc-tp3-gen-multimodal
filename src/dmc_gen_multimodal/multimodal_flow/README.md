# Multimodal Flow

Placeholder package for the future bridge from the tuned LLM output to the tuned diffusion model.

Intended responsibilities:

- read `image_prompt` from LLM campaign JSON
- normalize prompts for visual generation
- call the diffusion inference pipeline
